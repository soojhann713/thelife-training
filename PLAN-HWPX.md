# 과제 취합문서(HWPX) 생성 — 개발 계획

수집된 훈련 과제를 **교회 양식(.hwpx)** 에 채워 취합문서로 내려받는 기능의 설계·구현 계획서입니다.
실제 양식 파일 4종(설교간증 / 생활간증 / 독서과제 / 출석·과제현황)을 분석한 결과를 반영했습니다.

**대원칙 — 양식은 손대지 않는다.** 서식도, 양식 파일 자체도 수정하지 않습니다.
값이 들어갈 자리는 양식에 표식을 심는 대신 **문서 구조로 찾아냅니다**(§5).

## 1. 확정된 요구사항

| 항목 | 결정 |
| --- | --- |
| 양식 | **내용을 비운 공용 빈 양식 1개**를 저장소에 두고 가져다 씀 (§4) |
| 기수 구분 | 양식은 공용, **기수는 파일명에만** 넣음 |
| 실행 위치 | 웹 대시보드 버튼 → 브라우저에서 생성 → 즉시 다운로드 (서버 없음) |
| 취합 단위 | 과제(차수)별 문서 1개 — 한 과제에 대한 전 멤버 답안 |
| 문서 내용 | 양식이 정의하는 필드에 맞춰 입력 |
| 설교간증 | **주 2회(금요·주일)**, 과정 시작~종료까지 매주. `말씀은혜나눔` 카테고리 글로 등록 |

## 2. 양식 분석 결과

### 2.1 네 문서의 정체

| # | 문서 | 제출일 | 구성 |
| --- | --- | --- | --- |
| 1 | **설 교 간 증** (11기 제자 훈련반) | 2026-05-03 | 표 13개 (멤버 1인 = 표 1개) |
| 2 | **생 활 간 증** (11기 제자 훈련반) | 2026-05-03 | 표 13개 |
| 3 | **독 서 과 제** (11기 제자 훈련반) | 2026-04-26 | 표 13개 |
| 4 | **2026년 제11기 제자반(주일) 출석과 과제현황** | (누적) | 표 2개, 38행×55열 / 28행×55열 |

1~3번은 **표 구조가 완전히 동일**합니다(제목 문구만 다름). → 공용 양식 1개로 충분합니다.
4번은 성격이 다른 **현황판**이라 별도로 다룹니다(§7).

### 2.2 과제 취합문서의 구조 — 멤버 1인 = 페이지 1장

문서는 최상위 문단(`hp:p`) 13개로 되어 있고, **문단 하나가 표 하나를 감싸고** 있습니다.
2번째 블록부터 `pageBreak="1"` 이라 **멤버마다 페이지가 나뉩니다**.

```
hs:sec
└ hp:p (paraPrIDRef=16, pageBreak=0)      ← 멤버 1
  └ hp:tbl  4행 4열
     ┌─────────────────────────────┬────────┬──────────────────┐
     │ (0,0) 생 활 간 증(11기 …)    │ (0,2)  │ (0,3) 2026년 5월 3일 │
     │        colSpan=2 rowSpan=2  │ 제출일  ├──────────────────┤
     │                             │ rowSpan│ (1,3) 과제 내준 날짜 기록 │
     ├────────┬────────────────────┼────────┼──────────────────┤
     │ (2,0)  │ (2,1) 삼위일체 하나님… │ (2,2)  │ (2,3) 강성건       │
     │ 제목    │      ← 제목 값       │ 제출자  │      ← 이름        │
     ├────────┴────────────────────┴────────┴──────────────────┤
     │ (3,0) 본문  colSpan=4                                    │
     │   <hp:p> 문단 1 / <hp:p> 문단 2 …                        │
     └──────────────────────────────────────────────────────────┘
└ hp:p (pageBreak=1) ← 멤버 2 …
```

- 표 13개의 **제출자 순서가 코드의 `QT_TARGET_NAMES` 와 완전히 일치**합니다.
  `TARGET_NAMES` 에만 있는 **백지연은 이 문서들에 없습니다**.
- 본문은 **`hp:p` 하나 = 문단 하나**. 줄바꿈 문자가 아니라 문단 노드로 나뉩니다.
- **미제출자도 표를 그대로 남깁니다.** 제목·본문 셀이 텍스트 없는 빈 run 으로 비어 있습니다.
  → 계획의 미제출 처리 방침이 실제 관행과 일치.

### 2.3 '제목' 칸의 의미가 과제 종류마다 다르다 ⚠️

| 문서 | 제목 칸에 들어간 값 | 13명이 |
| --- | --- | --- |
| 생활간증 | `삼위일체 하나님의 구원사역으로 인한 감사와 변화` (= 커리큘럼 과제명) | **전원 동일** |
| 독서과제 | `하나님의 열심` (= 책 이름) | **전원 동일** |
| 설교간증 | `4월 26일 주일 설교 말씀` / `주일예배` / `열 번도 더 죽을 수 있는 죽음의 기쁨` … | **제각각** |

→ 생활간증·독서는 `task.title` 을 그대로 쓰고, **설교간증만 멤버별 게시글 제목**을 씁니다.

### 2.4 현재 문서에 남아 있는 오염

본문 곳곳에 `<o:p></o:p>` 가 박혀 있습니다. 게시판 글을 웹에서 복사해 한글에 붙여넣은 HTML
잔재입니다. 자동화하면 `content` 를 텍스트로 다루므로 **이 문제가 사라집니다**(개선점).

## 3. 우리 데이터 vs 양식이 요구하는 값

| 양식이 요구하는 값 | 우리가 가진 것 | 상태 |
| --- | --- | --- |
| 제출일 | 반별 마감일 `classes/<id>/due/<taskId>` | ✅ |
| 제목 (생활간증·독서) | `task.title` | ✅ |
| 제목 (설교간증) | 매칭된 글 제목 `post.title` | ✅ 분류 태그만 제거 |
| 제출자 13명, 고정 순서 | `QT_TARGET_NAMES` 와 **완전 일치** | ✅ |
| 본문 (문단 단위) | `post.content` | ✅ 문단 분할 필요 |
| 미제출 = 빈 셀 | `autoAssign` 미매칭 | ✅ |
| **설교간증 과제 자체** | 커리큘럼에 없음 | ❌ **추가 필요** (§3.1) |
| 출석·금요기도회·주일예배 | 없음 | ❌ **수집 불가** (§7) |

### 3.1 설교간증 — 커리큘럼에 추가해야 하고, 매칭 방식이 다르다

**사실관계** (확인된 것)

- 주 2회 — **금요일**(금요예배 말씀) + **주일**(주일예배 말씀)
- 과정 시작부터 종료까지 매주
- 게시판 카테고리는 **`말씀은혜나눔`**
- 제출일은 다음 강의일(주일). 실제 문서에서 `4월 26일 주일 설교 말씀` → 제출일 `2026년 5월 3일`

**문제 — 기존 키워드 매칭이 통하지 않습니다.**
지금 자동 체크는 과제별 키워드(`m`/`x`)로 **글 제목을 매칭**합니다. 그런데 설교간증 제목은
설교 제목 그 자체라 매주 달라지고 예측할 수 없습니다(`주일예배`, `열 번도 더 죽을 수 있는
죽음의 기쁨`, `우리의 연약함이 전도의 시작이 된다` …). 키워드 목록을 만들 수가 없습니다.

**제안 — 카테고리 + 날짜 기반 매칭** (설교간증 전용 경로)

```
말씀은혜나눔 카테고리 글 중,
  1순위: 제목에서 날짜를 파싱해(예: "4월 26일") 해당 예배일과 일치하는 글
  2순위: 날짜가 없으면 postDate 가 [예배일, 다음 예배일) 구간에 드는 글
```

제목 날짜 파싱은 `extractQtDays` 와 같은 형식들을 이미 다룰 수 있으므로 재사용 가능합니다.
과제 정의에는 키워드(`m`) 대신 **`service`(금/주일)** 와 **`serviceDate`(예배일)** 를 둡니다.

> ⚠️ **이 규칙은 실데이터로 검증해야 확정됩니다.** 이 세션에서는 RTDB 자격증명이 없어
> `말씀은혜나눔` 글의 실제 제목·`postDate` 분포를 볼 수 없었습니다. P0에서 확인합니다.
> 특히 **같은 주에 금요·주일 글 2건을 어떻게 가르는지**가 핵심입니다.

**남은 질문**: 방학(6/21~9/6) 기간에도 설교간증이 있는지 → §10 결정 1

## 4. 빈 양식 — `assets/templates/과제취합-빈양식.hwpx`

받은 양식(생활간증)에서 **내용만 지우고 서식은 그대로 둔** 껍데기를 만들어 커밋했습니다.
실명 간증문이 git 이력에 남지 않게 하기 위함이며, 서식은 원본과 동일합니다.

| 만든 방법 | 내용 |
| --- | --- |
| 남긴 것 | `Contents/header.xml` 전체(글꼴·문단모양·표 스타일), 표 1개, 셀 배치·`borderFillIDRef`·`paraPrIDRef`·`charPrIDRef` 전부 |
| 지운 것 | 표 12개(멤버 2~13번 블록), 모든 값 텍스트, `Preview/PrvText.txt` 내용, `Preview/PrvImage.png`, `content.hpf` 의 작성자·최종저장자·날짜 |
| 남은 텍스트 | `제출일` · `과제 내준 날짜 기록` · `제목` · `제출자` — **앵커로 쓰는 고정 라벨** |

내보낼 때 채우는 칸:

| 셀 | 값 |
| --- | --- |
| (0,0) | `생 활 간 증(11기 제자 훈련반)` — 과제 종류 + 기수 |
| (0,3) | 제출일 `2026년   5월  3일` |
| (2,1) | 제목 |
| (2,3) | 제출자 이름 |
| (3,0) | 본문 (문단들) |

> (0,0) 헤더 셀은 원래 양식에 문구가 박혀 있던 자리입니다. 공용 빈 양식을 쓰기로 했으므로
> **이 칸도 내보낼 때 채웁니다.** 문구는 원본 그대로(`설 교 간 증` / `생 활 간 증` /
> `독 서 과 제` + `(N기 제자 훈련반)`) 재현합니다. → §10 결정 2

검증 완료: 표 1개, 라벨 4개만 잔존, 전 엔트리에서 멤버 실명·작성자명 0건,
`mimetype` 이 첫 엔트리이자 무압축(STORED).
**한글에서 정상으로 열리는지는 사람이 확인해야 합니다**(§9 리스크 1).

## 5. 자동화 방식 — 양식에 표식을 심지 않는다

양식을 수정할 수 없으므로 `{{이름}}` 같은 토큰을 넣을 수 없습니다.
대신 **양식의 구조가 이미 충분히 규칙적이라는 점을 이용**합니다.

### 5.1 앵커 기반 셀 찾기

좌표를 하드코딩하지 않고 **라벨 셀을 앵커로 삼아 값 셀을 찾습니다.**

| 앵커(고정 라벨) | 값을 넣을 곳 |
| --- | --- |
| `제출일` | 같은 표에서 그 다음에 오는 셀 |
| `제목` | 그 다음 셀 |
| `제출자` | 그 다음 셀 |
| (앵커 없음) | 표의 **첫 셀** = 문서 제목 / **`colSpan=4` 셀** = 본문 |

라벨을 못 찾으면 **에러로 처리**합니다. 조용히 엉뚱한 칸에 쓰는 것보다 낫습니다.

### 5.2 블록 복제로 인원 맞추기

빈 양식의 **최상위 문단 블록(표를 감싼 `hp:p`)** 을 프로토타입으로 삼아 멤버 수만큼 복제하고,
2번째부터 `pageBreak="1"` 을 켭니다. 복제본은 셀 구조와 모든 서식 ID 를 그대로 물려받으므로
**서식이 원본과 100% 같습니다.**

### 5.3 본문 문단 채우기

본문 셀 안의 **첫 `hp:p` 를 문단 틀로 복제**해 줄 수만큼 만들고, 각 복제본의 `hp:t` 에 한 줄씩
넣습니다. 원본 문단의 `paraPrIDRef`/`charPrIDRef` 가 복사되므로 본문 서식이 유지됩니다.
빈 줄은 텍스트 없는 문단 하나로 둡니다(문단 간격 유지).

### 5.4 손대는 것 / 안 건드리는 것

| 다루는 방식 | 대상 |
| --- | --- |
| **절대 안 건드림** | `Contents/header.xml`, `settings.xml`, `version.xml`, `META-INF/*`, `Scripts/*` |
| 값만 교체 | `Contents/section0.xml` 의 `hp:t` 텍스트 |
| 구조 복제 | 최상위 `hp:p` 블록(표 포함), 본문 `hp:p`(문단) |
| 재생성 | `hp:tbl/@id` (문서 내 고유값이라 복제 시 충돌 방지) |
| 삭제 | 수정한 문단의 `hp:linesegarray` (줄배치 캐시 — 한글이 열 때 다시 계산) |
| 갱신 | `Preview/PrvText.txt` 를 생성 평문으로 덮어씀 |

### 5.5 ZIP 재작성 규칙

`mimetype` 을 **첫 엔트리·무압축(STORED)** 으로 두고 나머지를 다시 담습니다.
양식에 있던 엔트리는 하나도 빠뜨리지 않습니다(`Scripts/`, `container.rdf` 포함).

### 5.6 파일명

기수를 파일명에 넣습니다. 예: `11기_생활간증_20260503.hwpx`

## 6. 모듈 구성

```
assets/templates/과제취합-빈양식.hwpx   ← 공용 빈 양식 (커밋 완료)
js/hwpx/zip.js        ZIP 읽기/쓰기 (JSZip 지연 로드, 구현 주입 가능)
js/hwpx/owpml.js      블록/문단 복제, 앵커로 셀 찾기, 텍스트 치환   ← 순수 함수
js/hwpx/compile.js    취합 데이터 → 채워진 section0.xml            ← 순수 함수
js/hwpx/build.js      양식 fetch → 해제 → compile → 재압축 → Blob
js/hwpx/export-ui.js  내보내기 모달 (반·과제 선택, 사전 점검, 다운로드)
index.html            모달 마크업 + 대시보드 버튼
```

**설계 제약 두 가지**

1. `owpml.js` / `compile.js` 는 DOM·Firebase·JSZip 을 import 하지 않는 **순수 모듈**.
   → Node 테스트가 브라우저 파일을 **그대로 import** 하므로 CLAUDE.md 불변식 1의
   웹/Node 수동 동기화 문제를 새로 만들지 않습니다. (코드 복제 금지)
2. **ZIP 구현은 주입.** 브라우저는 CDN 의 `window.JSZip`(첫 클릭 시 지연 로드),
   Node 테스트는 devDependency `jszip`. 순수 모듈은 ZIP 을 모릅니다.

XML 은 `DOMParser` 없이 **문자열 기반 노드 슬라이싱**으로 다룹니다(Node 에서도 돌아야 하므로).
대상이 `hp:p` / `hp:tbl` / `hp:tc` 경계 찾기와 복제뿐이라 파서가 필요 없습니다.
중첩된 `hp:p`(셀 안의 문단)와 구분해야 하므로 **깊이 추적**은 필수입니다.

### 6.1 데이터는 추가 조회 없이 재사용

| 필요한 것 | 이미 있는 것 |
| --- | --- |
| 반 → 멤버 | `memberList` + `memberClassId(m)` (`active` 필터) |
| 반 → 과제 | `effectiveTasks(courseId, classId)` (반별 마감일 반영) |
| 과제 → 매칭된 글 | `autoAssign[name][taskId]` → `{title, postDate, link, content}` |
| 수동 체크 내용 | `assignStatus[name][taskId]` → `assignContent(v)` |
| 반/과정 이름 | `classesById`, `coursesById` |

```
buildTaskDoc({ task, classId, members, autoAssign, assignStatus })
  → { 문서제목, 제출일, rows: [{ 이름, 제목, 본문[] }] }   // 미제출은 제목·본문 빈 값
```

정렬은 이름 `localeCompare(ko)` — 과제 완주 현황 표와 같고, 양식의 13명 순서와도 일치합니다.

## 7. 출석·과제현황표는 별개 과제

표 2개에 38행×55열. **멤버 1명 = 4열**, 주차 1개 = 2행입니다.

```
과제행:  [생]  [큐티 일수 (colSpan=2)]  [독]
출석행:  [출]  [금 (colSpan=2)]         [주]
```

완료 여부는 글자를 지우는 게 아니라 **글자 스타일(`charPrIDRef`)로 표시**합니다.

| charPr | 스타일 | 의미 |
| --- | --- | --- |
| `13` | 검정 굵게 `#000000` | **완료** |
| `20` | 연회색 `#BFBFBF` | **미완료** |
| `32` | 연회색 + 취소선 | **해당 없음/제외** |

자동화는 **`charPrIDRef` 를 13 ↔ 20 으로 바꾸고 큐티 칸에 일수를 쓰는 것**이 전부라 아주 깔끔합니다.

**다만 지금은 만들 수 없습니다.** 6칸 중 3칸의 데이터가 없습니다.

| 칸 | 데이터 | 상태 |
| --- | --- | --- |
| 생 / 독 | `autoAssign` | ✅ |
| 큐티 일수 | `memberQtData` 주차별 집계 | ✅ |
| **출**(출석) / **금**(금요기도회) / **주**(주일예배) | — | ❌ 사람이 입력해야 하는 값 |

→ **출석 입력 기능이 먼저** 있어야 합니다. 이번 범위에서 제외하고 후속 과제로 둡니다.

이 표의 이름 행에는 `임채환 (반장)`, `한수종(총무)` 처럼 **직책이 붙어** 있습니다.
`/members` 에는 직책 필드가 없으므로, **양식의 이름 행을 그대로 두고** 괄호를 떼서 우리 명단과
대조해 열 번호를 찾는 방식이 맞습니다.

## 8. 단계별 진행

| 단계 | 내용 | 완료 조건 |
| --- | --- | --- |
| **P0** 준비 | ✅ 빈 양식 생성 · ⬜ **한글에서 열리는지 확인** · ⬜ 실데이터로 `말씀은혜나눔` 글 제목/날짜 분포 확인 → 설교간증 매칭 규칙 확정 · ⬜ 설교간증 과제를 커리큘럼에 추가 | 빈 양식이 한글에서 열리고, 설교간증 매칭 규칙이 실데이터로 검증됨 |
| **P1** 코어 | `owpml.js` — 앵커 셀 찾기·블록 복제·문단 복제·id 재생성·linesegarray 처리 | Node 테스트 통과 + **생성물이 한글에서 원본과 같은 모양** |
| **P2** 패키징 | `zip.js` + `build.js` | 엔트리 구성이 양식과 일치 |
| **P3** 데이터 | `buildTaskDoc()` — 설교간증 제목·매칭 분기 포함 | 제출/미제출/본문없음이 테스트로 고정 |
| **P4** UI | 내보내기 모달(반·과제 선택, 멤버별 매칭 글 미리보기, 사전 점검, 다운로드) | 클릭 → 파일 저장 |
| **P5** 검증·문서 | 실데이터 생성 → 기존 수작업 문서와 대조, README | **한글에서 열어 기존 문서와 같은 모양 확인** |

P1 이 이 작업의 심장입니다. 여기서 한글 호환성이 판가름 나므로 **끝나는 즉시 사람이 열어봅니다.**

## 9. 리스크

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| `linesegarray` 를 지웠을 때 한글이 줄배치를 다시 계산하지 않음 | 문서가 깨져 보임 | **P0/P1 최우선 검증.** 실패하면 문단 수를 바꾸지 않는 대안 검토 |
| 본문 길이가 바뀌어도 `cellSz/@height` 가 그대로라 표가 잘림 | 내용 잘림 | 긴 본문·짧은 본문 양쪽으로 확인. 필요하면 height 를 비우거나 추정 |
| 설교간증 매칭이 금요/주일을 잘못 가름 | 엉뚱한 글이 들어감 | 실데이터로 규칙 검증 + 모달에서 **매칭된 글 제목을 먼저 보여주고** 확인 후 다운로드 |
| `hp:tbl/@id` 중복 | 한글이 파일을 거부 | 복제 시 새 id 발급 + 중복 검사 테스트 |
| 문자열 XML 조작의 엣지 케이스 (중첩 `hp:p`) | 문서 손상 | 깊이 추적 + 자기닫힘 태그를 픽스처로 고정, XML well-formed 검사 |
| `mimetype` 순서/압축 위반 | 파일 열림 실패 | `zip.js` 에서 강제 + 결과를 테스트로 검증 |
| 과거 글의 `content` 가 빔 | 취합 누락 | 사전 점검에서 건수 경고 + `backfill-content` 안내 |
| JSZip CDN 장애 | 기능 정지 | 버전 고정 + 실패 시 명확한 안내 |

## 10. 결정 대기 항목

| # | 질문 | 기본안 |
| --- | --- | --- |
| 1 | 방학(6/21~9/6)에도 설교간증이 있는가 | 있다고 보고 전 주차 생성. 아니면 학기 구간만 |
| 2 | 헤더 셀 문구를 코드가 만들어도 되는가 | 됨. `설 교 간 증`/`생 활 간 증`/`독 서 과 제` + `(N기 제자 훈련반)` 을 원본 그대로 재현 |
| 3 | 백지연 | 양식 13명에 없음 = `QT_TARGET_NAMES` 기준. 취합 대상도 동일하게 |
| 4 | 내보내기 권한 | 관리자 = 전 반, 반 계정 = 자기 반만 (기존 스코프 그대로, 읽기 전용이라 **RTDB 규칙 변경 없음**) |

### 확정된 것 (이전 결정 대기 항목)

- 양식 = **공용 빈 양식 1개**, 기수는 파일명에만 → §1, §4
- 저장소에는 **내용을 비운 껍데기**만 커밋 → §4 (완료)
- 설교간증 **포함**, 주 2회(금·주일), `말씀은혜나눔` 카테고리 → §3.1
- 미제출자는 표를 남기고 제목·본문만 빔 → 실제 양식 관행과 일치
- 첨부 이미지(`BinData/`) 는 이번 범위 제외

## 11. 테스트

- **Node 단위 테스트** (`scripts/test/hwpx-*.test.js`, 기존 `node --test` 에 자동 편입)
  - `owpml.js`: 앵커 셀 찾기(라벨 없으면 throw), 블록 복제, 문단 복제, 중첩 `hp:p` 깊이 추적,
    id 재생성, 이스케이프, 제어문자 제거, 자기닫힘 태그
  - `compile.js`: 빈 양식 → 기대 출력, 블록 수 = 멤버 수, `pageBreak` 설정, 미제출 시 빈 run 유지
  - `buildTaskDoc()`: 제출/미제출/본문없음/설교간증 제목 분기/정렬
  - 설교간증 매칭: 제목에 날짜 있음/없음, 같은 주 금요·주일 2건 구분
  - 패키징: 엔트리 목록·`mimetype` 첫 엔트리·STORED·XML well-formed
- **픽스처**: `assets/templates/과제취합-빈양식.hwpx` 를 그대로 사용 (표 1개짜리라 이미 최소)
- **devDependency**: `scripts/package.json` 에 `jszip` (테스트 전용)
- **수동 검증**: 생성 문서를 한글에서 열어 **기존 수작업 문서와 나란히 비교**

CI 는 기존 `ci.yml` 그대로 통과해야 합니다 (`node --check` 전체 + `npm test`).

## 12. 다음 액션

1. **빈 양식을 한글에서 열어 확인** — 이게 통과해야 나머지가 의미가 있습니다
2. `말씀은혜나눔` 글의 실제 제목·`postDate` 확인 → 설교간증 매칭 규칙 확정 (§3.1)
3. §10 결정 1·3 확인
