# 과제 취합문서(HWPX) 생성 — 개발 계획

수집된 훈련 과제를 **교회 양식(.hwpx)** 에 채워 넣어 취합문서로 내려받는 기능의 설계·구현 계획서입니다.
구현 전 합의용 문서이며, 확정되지 않은 항목은 [§8 결정 대기](#8-결정-대기-항목) 에 모았습니다.

## 1. 목표와 범위

**한 줄 요약** — 대시보드에서 `반 + 과제(차수)` 를 고르고 버튼을 누르면, 그 과제에 대한 전 멤버의
제출 내용이 교회 양식 서식 그대로 채워진 `.hwpx` 파일이 즉시 내려받아진다.

확정된 요구사항 (사용자 확인):

| 항목 | 결정 |
| --- | --- |
| 양식 | **기존 교회 양식 `.hwpx` 파일을 제공받아 그대로 재사용** (서식을 새로 만들지 않음) |
| 실행 위치 | **웹 대시보드 버튼 → 브라우저에서 생성 → 즉시 다운로드** (서버 없음, 현재 구조 유지) |
| 취합 단위 | **과제(차수)별 문서 1개** — 예: “사역11기 3주차 생활간증” 한 과제에 대한 전 멤버 답안 |
| 문서 내용 | **양식이 정의하는 필드에 맞춰 입력** (내용 구성을 새로 설계하지 않고 양식을 따름) |

### 범위 밖 (이번 작업에서 제외)

- 게시글 **첨부 이미지/파일** 삽입 (`BinData/`) — 본문 텍스트만. → [§8](#8-결정-대기-항목)
- 멤버별 개별 문서, 반 전체 통합 문서 — 코어가 완성되면 진입점만 추가하면 되므로 후속 과제로 둠.
- `.hwp`(바이너리 구형 포맷), PDF 변환.
- 큐티(QT) 취합 — 이번엔 **과제(훈련나눔)** 만.

## 2. 왜 이 구조인가

이 저장소는 **빌드 단계가 없고 서버가 없다** (CLAUDE.md). 이 제약이 설계를 대부분 결정합니다.

- **브라우저 생성이 자연스럽다.** 대시보드는 이미 `loadAllPosts()` 로 스코프 내 전 멤버의 글을
  `content` 까지 포함해 메모리에 올려둔다. 취합에 필요한 데이터는 **추가 조회 없이 이미 손에 있다.**
- **Actions 잡은 UX가 나쁘다.** 파일을 받으려면 Actions 페이지 → 아티팩트 다운로드를 거쳐야 한다.
  관리자가 원할 때 바로 뽑는 흐름이 목적이므로 웹 버튼이 맞다.
- **양식 재사용이 서식 리스크를 없앤다.** 서식(글꼴·문단모양·표 스타일)은 전부 `Contents/header.xml`
  과 각 요소의 `charPrIDRef`/`paraPrIDRef` 에 있다. 양식 파일의 `header.xml` 을 **손대지 않고**
  본문(`Contents/section0.xml`)만 채우면, 한글에서 열었을 때 서식이 원본과 동일하다.
  HWPX 를 처음부터 생성하면 한글 호환성 검증 부담이 훨씬 커진다.

## 3. HWPX(OWPML) 구조 — 다뤄야 하는 것

`.hwpx` 는 ZIP 컨테이너다 (KS X 6101 / OWPML). 양식 파일의 엔트리 구성:

```
mimetype                     application/hwp+zip  ← 반드시 첫 엔트리, 무압축(STORED)
META-INF/container.xml       루트 파일 경로
META-INF/manifest.xml        엔트리 목록/MIME
Contents/content.hpf         OPF 형식 컨텐츠 목록 (섹션 파일 나열)
Contents/header.xml          문서 설정: 글꼴, 스타일, charPr/paraPr, 용지 → 손대지 않음
Contents/section0.xml        본문: 문단·표·이미지          → 이것만 생성
Preview/PrvText.txt          미리보기 텍스트 (내용과 불일치하면 갱신/삭제)
Preview/PrvImage.png         미리보기 이미지 (삭제 → 한글이 재생성)
BinData/                     이미지 등 바이너리 (이번 범위 밖)
```

본문 XML 은 네임스페이스 접두사를 쓴다: `hs:` 섹션, `hp:` 문단/런, `hh:` 헤더, `hc:` 공통.
문단 구조는 `hs:sec > hp:p > hp:run > hp:t` 이고, 각 `hp:p` 는 `paraPrIDRef`,
각 `hp:run` 은 `charPrIDRef` 로 `header.xml` 의 서식을 참조한다.

> ⚠️ **핵심 원칙**: 우리는 XML 을 *새로 쓰지 않고*, 양식의 문단/표 노드를 **복제해서 텍스트만 바꾼다.**
> `paraPrIDRef`/`charPrIDRef` 를 직접 만들어내는 순간 서식 보장이 깨진다.

## 4. 템플릿 채우기 방식 — mustache-lite

양식 파일에 **토큰**을 심어두고 치환한다. 토큰은 한글에서 그냥 텍스트로 보이므로,
담당자가 한글에서 양식을 직접 열어 수정·유지할 수 있다.

### 4.1 스칼라 토큰

`hp:t` 텍스트 노드 안의 `{{...}}` 를 값으로 치환.

| 토큰 | 값 |
| --- | --- |
| `{{반}}` | 반 이름 (`classes/<id>/label`, 예: 사역11기) |
| `{{커리큘럼}}` | 과정 이름 (`courses/<id>/label`) |
| `{{과제}}` | 과제 제목 (`task.title`) |
| `{{과제종류}}` | `task.kind` (생활간증 / 독서 / 기타) |
| `{{주차}}` | `task.week` (있는 경우) |
| `{{강의}}` | `task.lecture` (있는 경우) |
| `{{마감일}}` | 반별 마감일 (`classes/<id>/due/<taskId>` → 없으면 커리큘럼 기본값) |
| `{{생성일}}` | 문서 생성일 (KST) |
| `{{제출수}}` / `{{대상수}}` | 제출 인원 / 대상 인원 |

### 4.2 반복 블록

멤버 수만큼 반복될 구간을 양식에서 표시한다. 두 형태를 지원한다.

- **문단형** — `{{#멤버}}` 만 있는 문단 ~ `{{/멤버}}` 만 있는 문단 **사이의 노드들**을 1인분
  블록으로 보고, 멤버마다 복제한 뒤 마커 문단 2개를 제거.
- **표형** — 표(`hp:tbl`) 안에서 `{{#멤버}}` 를 포함한 **행(`hp:tr`)** 을 1인분으로 보고 행을 복제.

1인분 블록 안에서 쓰는 토큰:

| 토큰 | 값 |
| --- | --- |
| `{{번호}}` | 1부터의 순번 |
| `{{이름}}` | 멤버 이름 |
| `{{글제목}}` | 매칭된 글 제목 |
| `{{작성일}}` | `postDate` (`YYYY.MM.DD`) |
| `{{원문링크}}` | 게시글 URL |
| `{{본문}}` | 글 본문 전문 (여러 문단) |
| `{{상태}}` | `제출` / `미제출` |

### 4.3 여러 줄 본문 — 반드시 문단 복제로

`{{본문}}` 은 개행이 포함된 긴 텍스트다. `hp:t` 안에 `\n` 을 그대로 넣으면 한글에서 줄이 나뉘지 않는다.
→ **`{{본문}}` 을 담은 `hp:p` 노드를 줄 수만큼 복제**하고, 각 복제본의 `hp:t` 에 한 줄씩 넣는다.
원본 문단의 `paraPrIDRef`/`charPrIDRef` 가 그대로 복사되므로 본문 서식이 유지된다.
빈 줄은 텍스트 없는 문단 하나로 (문단 간격 유지).

### 4.4 정리 규칙

- XML 이스케이프(`& < > " '`) 후, XML 1.0 에서 허용되지 않는 제어문자 제거.
- 치환 후 문서에 `{{` 가 남아 있으면 **에러로 처리** (양식/코드 불일치를 조용히 넘기지 않는다).
- `Preview/PrvText.txt` 는 생성된 평문으로 덮어쓰고, `Preview/PrvImage.png` 는 엔트리에서 제거.
- 섹션은 `section0.xml` 하나만 쓴다 → `content.hpf` / `manifest.xml` 수정 불필요.
- ZIP 재작성 시 **`mimetype` 을 첫 엔트리·무압축(STORED)** 으로. 나머지는 DEFLATE.

## 5. 데이터 매핑 — 기존 상태를 그대로 재사용

`js/dashboard.js` 가 이미 갖고 있는 것:

| 필요한 것 | 이미 있는 것 |
| --- | --- |
| 반 → 멤버 목록 | `memberList` + `memberClassId(m)` (`active` 필터) |
| 반 → 커리큘럼 과제 | `effectiveTasks(memberCourseId(m), memberClassId(m))` (반별 마감일 반영) |
| 과제 → 자동 매칭된 글 | `autoAssign[name][taskId]` — `buildAutoAssign()` 결과, `{title, postDate, link, content}` |
| 수동 체크 과제 내용 | `assignStatus[name][taskId]` → `assignContent(v)` |
| 반/과정 이름 | `classesById`, `coursesById` |

취합 데이터 빌더는 순수 함수로 분리한다:

```
buildTaskDoc({ classLabel, courseLabel, task, members, autoAssign, assignStatus })
  → { meta: {...}, rows: [{ no, name, status, postTitle, postDate, link, body }] }
```

**미제출/본문 없음 처리** (기본안, [§8](#8-결정-대기-항목) 에서 확정):

- 매칭된 글도 수동 내용도 없음 → `상태 = 미제출`, 본문 자리는 비우고 행은 **남긴다**
  (취합 누락이 한눈에 보이도록).
- 매칭된 글은 있는데 `content` 가 빈 문자열 → `상태 = 제출(본문 없음)` 로 표시하고 원문 링크를 남긴다.
  본문은 수집 시점에 함께 저장되지만(`collect-daily.js`), 과거 글은 비어 있을 수 있다.
  → 내보내기 전 **사전 점검**에서 "본문 없는 제출 N건" 을 경고하고
  `backfill-content` 워크플로우 실행을 안내한다.

정렬은 이름 `localeCompare(ko)` — 과제 완주 현황 표와 동일하게.

## 6. 모듈 구성

```
assets/templates/과제취합양식.hwpx     ← 제공받은 양식 (바이너리, 저장소에 커밋)
js/hwpx/zip.js        ZIP 읽기/쓰기 (JSZip 지연 로드 + 주입 가능한 인터페이스)
js/hwpx/owpml.js      OWPML 조작: 이스케이프, 토큰 치환, 문단/행 복제  ← 순수 함수
js/hwpx/compile.js    데이터 → 채워진 section0.xml                     ← 순수 함수
js/hwpx/build.js      양식 fetch → 압축 해제 → compile → 재압축 → Blob
js/hwpx/export-ui.js  내보내기 모달 (반·과제 선택, 사전 점검, 다운로드)
index.html            내보내기 모달 마크업 + 대시보드 버튼
```

### 설계 제약 두 가지

1. **`owpml.js` / `compile.js` 는 DOM·Firebase·JSZip 을 import 하지 않는다.**
   문자열/객체만 다루는 순수 모듈이면 Node 테스트가 브라우저 파일을 **그대로 import** 할 수 있다
   → CLAUDE.md 불변식 1의 “웹/Node 코드 수동 동기화” 문제를 아예 만들지 않는다. (복제 금지)
2. **ZIP 구현은 주입한다.** 브라우저는 CDN 의 `window.JSZip`, Node 테스트는 devDependency `jszip`.
   `zip.js` 가 이 차이를 흡수하고, 위 순수 모듈은 ZIP 을 모른다.

### JSZip 로딩

`index.html` 에 미리 넣지 않고 **첫 내보내기 클릭 시 동적으로 `<script>` 주입**
(`https://cdnjs.cloudflare.com/ajax/libs/jszip/3.10.1/jszip.min.js`, 버전 고정).
대시보드 초기 로딩 비용 0. Firebase 를 gstatic CDN 에서 불러오는 기존 방식과 일관.

> 대안: 라이브러리 없이 `DecompressionStream('deflate-raw')` / `CompressionStream` + 자체 ZIP
> 리더/라이터 (~200줄). 외부 의존성이 0 이 되지만 코드량과 검증 부담이 늘어 **1차에서는 JSZip 채택.**

### XML 파싱

브라우저 `DOMParser`/`XMLSerializer` 는 Node 에 없다. 순수 모듈을 Node 에서 테스트하려면
DOM 없이 동작해야 한다 → **정규식/문자열 기반 노드 슬라이싱**으로 구현한다
(대상이 `{{토큰}}` 을 포함한 `hp:p` / `hp:tr` 의 경계 찾기 + 복제라 파서가 필요 없다).
태그 균형은 여는/닫는 태그 카운팅으로 처리하고, `<hp:p ... />` 자기닫힘 형태를 반드시 테스트한다.

## 7. 단계별 진행

| 단계 | 내용 | 산출물 | 완료 조건 |
| --- | --- | --- | --- |
| **P0** 양식 분석 | 제공받은 `.hwpx` 압축 해제, 엔트리·`section0.xml` 구조·표/문단 배치 파악. 토큰 삽입 위치 확정 후 양식 파일에 토큰을 심어 저장 | 구조 분석 메모 + 토큰이 심어진 양식 파일 | 토큰 목록이 §4 표로 확정 |
| **P1** 코어 | `owpml.js` + `compile.js` (토큰 치환·블록 복제·본문 문단 분할) | 순수 모듈 + Node 테스트 | 픽스처 양식 → 채워진 `section0.xml`, 미치환 토큰 0 |
| **P2** 패키징 | `zip.js` + `build.js` (해제→치환→재압축, mimetype 규칙, Preview 처리) | `.hwpx` Blob 생성 | 생성 파일이 다시 열리고 엔트리 구성이 양식과 일치 |
| **P3** 데이터 | `buildTaskDoc()` — 과제별 취합 데이터 빌더 | 순수 함수 + 테스트 | 미제출·본문없음 케이스가 테스트로 고정 |
| **P4** UI | 내보내기 모달(반·과제 드롭다운, 제출/미제출 요약, 사전 점검 경고, 다운로드), 대시보드 버튼 | `export-ui.js` + `index.html` + CSS | 로그인 상태에서 클릭 → 파일 저장 |
| **P5** 검증·문서 | 한글에서 실제 열기 확인, README 사용법, CI 통과 | 문서 + 통과하는 CI | **사용자가 한글에서 열어 서식 확인 OK** |

P1~P3 은 P0 산출물에 의존한다. **P0 없이는 시작할 수 없다** — 양식 파일이 첫 준비물이다.

## 8. 결정 대기 항목

| # | 질문 | 기본안(합의 없으면 이대로) |
| --- | --- | --- |
| 1 | 양식의 1인분 블록이 **표 행**인가 **문단 묶음**인가 | 양식 파일 확인 후 결정. 둘 다 지원 구현 |
| 2 | 토큰을 **내가 양식에 심어도** 되는가, 담당자가 심는가 | 내가 심어 커밋하고, README 에 토큰 목록을 문서화 |
| 3 | **미제출자**를 문서에 남기는가 | 남긴다 (`상태 = 미제출`) |
| 4 | 게시글 **첨부 이미지** 포함 | 1차 제외 (본문 텍스트만) |
| 5 | 파일명 규칙 | `{반}_{주차}주차_{과제종류}_취합_{생성일}.hwpx` <br> 예: `사역11기_3주차_생활간증_취합_20260831.hwpx` |
| 6 | 내보내기 **권한** | 관리자 = 전체 반, 반 계정 = 자기 반만 (기존 스코프 규칙 그대로, **RTDB 규칙 변경 불필요** — 읽기 전용 기능) |
| 7 | 양식 파일 위치 | `assets/templates/` 에 커밋 (GitHub Pages 가 정적 파일로 서빙 → `fetch()` 로 로드) |

## 9. 리스크

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| 한글이 생성 파일을 못 열거나 서식이 깨진다 | 치명적 | `header.xml` 무수정 + 양식 노드 복제 원칙. **P5 에서 사람이 한글로 직접 열어 확인** (CI 로는 검증 불가) |
| `mimetype` 엔트리 순서/압축 방식 위반 | 파일 열림 실패 | `zip.js` 에서 강제 + 재압축 결과의 첫 엔트리·압축방식을 테스트로 검증 |
| 문자열 기반 XML 조작이 엣지 케이스에서 깨짐 | 문서 손상 | 자기닫힘 태그·중첩 표·속성 내 `{{` 를 테스트 픽스처로 고정. 미치환 토큰 잔존 시 에러 |
| 과거 글의 `content` 가 비어 있음 | 취합 누락 | 사전 점검에서 건수 경고 + `backfill-content` 안내 |
| 본문이 매우 길어 파일이 커짐 | 성능 | 과제별 단위라 1문서 ≈ 멤버 수(10~15명)분 → 문제 없는 규모. 생성은 비동기 + 진행 표시 |
| 자동 매칭 오류로 엉뚱한 글이 들어감 | 내용 오류 | 모달에서 **멤버별 매칭된 글 제목을 미리 보여주고** 확인 후 다운로드 |
| JSZip CDN 장애 | 기능 정지 | 버전 고정 + 로드 실패 시 명확한 안내 메시지. (필요해지면 §6 의 무의존성 대안으로 교체) |

## 10. 테스트

- **Node 단위 테스트** (`scripts/test/hwpx-*.test.js`, 기존 `node --test` 에 자동 편입):
  - `owpml.js`: 토큰 치환, 문단/행 복제, 여러 줄 본문 분할, 이스케이프, 제어문자 제거, 자기닫힘 태그
  - `compile.js`: 픽스처 `section0.xml` → 기대 출력, 미치환 토큰 검출 시 throw
  - `buildTaskDoc()`: 제출/미제출/본문없음/정렬
  - 패키징: 생성 ZIP 의 엔트리 목록·`mimetype` 첫 엔트리·STORED·XML well-formed
- **픽스처**: 실제 양식에서 잘라낸 최소 `.hwpx` 를 `scripts/test/fixtures/` 에 커밋
  (실제 양식 전체를 테스트에 쓰면 실패 원인 파악이 어렵다)
- **devDependency 추가**: `scripts/package.json` 에 `jszip` (테스트 전용)
- **수동 검증**: 실제 데이터로 생성 → **한글에서 열어 서식·페이지 나눔·글꼴 확인** (P5 완료 조건)

CI 는 기존 `ci.yml` 그대로 통과해야 한다 (`node --check` 전체 + `npm test`).

## 11. 지금 필요한 것

**교회 과제 취합 양식 `.hwpx` 파일 1개.** 이것만 있으면 P0 부터 바로 진행한다.
파일을 받으면 구조를 분석해 §4 토큰 표와 §8 의 1·2·5번을 확정한 뒤 구현에 들어간다.
