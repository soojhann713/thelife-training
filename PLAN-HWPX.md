# 과제 취합문서(HWPX) 생성 — 개발 계획

수집된 훈련 과제를 **교회 양식(.hwpx)** 에 채워 취합문서로 내려받는 기능의 설계·구현 계획서입니다.
실제 양식 파일 4종(설교간증 / 생활간증 / 독서과제 / 출석·과제현황)을 분석한 결과를 반영했습니다.

**대원칙 — 양식은 손대지 않는다.** 서식도, 양식 파일 자체도 수정하지 않습니다.
값이 들어갈 자리는 양식에 표식을 심는 대신 **문서 구조로 찾아냅니다**(§4).

## 1. 확정된 요구사항

| 항목 | 결정 |
| --- | --- |
| 양식 | 기존 교회 양식 `.hwpx` 를 **수정 없이 그대로** 사용 |
| 실행 위치 | 웹 대시보드 버튼 → 브라우저에서 생성 → 즉시 다운로드 (서버 없음) |
| 취합 단위 | 과제(차수)별 문서 1개 — 한 과제에 대한 전 멤버 답안 |
| 문서 내용 | 양식이 정의하는 필드에 맞춰 입력 |

## 2. 양식 분석 결과

### 2.1 네 문서의 정체

| # | 문서 | 제출일 | 구성 |
| --- | --- | --- | --- |
| 1 | **설 교 간 증** (11기 제자 훈련반) | 2026-05-03 | 표 13개 (멤버 1인 = 표 1개) |
| 2 | **생 활 간 증** (11기 제자 훈련반) | 2026-05-03 | 표 13개 |
| 3 | **독 서 과 제** (11기 제자 훈련반) | 2026-04-26 | 표 13개 |
| 4 | **2026년 제11기 제자반(주일) 출석과 과제현황** | (누적) | 표 2개, 38행×55열 / 28행×55열 |

1~3번이 우리가 만들려는 **과제 취합문서**이고, 성격이 완전히 같습니다.
4번은 성격이 다른 **현황판**이라 별도로 다룹니다(§6).

### 2.2 과제 취합문서(1~3번)의 표 구조 — 완벽하게 규칙적

멤버 1인당 표 1개, **모든 표가 4행 4열로 동일**합니다.

```
┌─────────────────────────────┬────────┬──────────────────┐
│ (0,0) 생 활 간 증(11기 …)    │ (0,2)  │ (0,3) 2026년 5월 3일 │
│        colSpan=2 rowSpan=2  │ 제출일  ├──────────────────┤
│                             │ rowSpan│ (1,3) 과제 내준 날짜 기록 │
├────────┬────────────────────┼────────┼──────────────────┤
│ (2,0)  │ (2,1) 삼위일체 하나님… │ (2,2)  │ (2,3) 강성건       │
│ 제목    │      ← 제목 값       │ 제출자  │      ← 이름        │
├────────┴────────────────────┴────────┴──────────────────┤
│ (3,0) 본문  colSpan=4                                    │
│   <hp:p> 문단 1                                          │
│   <hp:p> 문단 2  …                                       │
└──────────────────────────────────────────────────────────┘
```

- 표 13개의 **제출자 순서가 코드의 `QT_TARGET_NAMES` 와 완전히 일치**합니다
  (강성건·서승민·양지혜·유정인·이소현·이재황·임채환·최연희·최지인·한상필·한수종·홍종성·황미진).
  `TARGET_NAMES` 에만 있는 **백지연은 이 문서들에 없습니다**.
- 본문은 **`hp:p` 하나 = 문단 하나**. 줄바꿈 문자가 아니라 문단 노드로 나뉩니다.
- **미제출자도 표를 그대로 남깁니다.** 제목·본문 셀이 텍스트 없는 빈 run
  (`<hp:run charPrIDRef="8"/>`) 으로 비어 있습니다. → 계획의 미제출 처리 방침이 실제 관행과 일치.

### 2.3 '제목' 칸의 의미가 과제 종류마다 다르다 ⚠️

| 문서 | 제목 칸에 들어간 값 | 13명이 |
| --- | --- | --- |
| 생활간증 | `삼위일체 하나님의 구원사역으로 인한 감사와 변화` (= 커리큘럼 과제명) | **전원 동일** |
| 독서과제 | `하나님의 열심` (= 책 이름) | **전원 동일** |
| 설교간증 | `4월 26일 주일 설교 말씀` / `주일예배` / `열 번도 더 죽을 수 있는 죽음의 기쁨` … | **제각각** |

→ 생활간증·독서는 `task.title` 을 그대로 쓰고, **설교간증만 멤버별 게시글 제목**을 써야 합니다.

### 2.4 현재 문서에 남아 있는 오염

본문 곳곳에 `<o:p></o:p>` 가 그대로 박혀 있습니다. 게시판 글을 웹에서 복사해 한글에 붙여넣은
HTML 잔재입니다. 자동화하면 `content` 를 텍스트로 다루므로 **이 문제가 사라집니다**(개선점).

## 3. 우리 데이터 vs 양식이 요구하는 값

| 양식이 요구하는 값 | 우리가 가진 것 | 상태 |
| --- | --- | --- |
| 문서 종류 3종 (설교간증/생활간증/독서과제) | `kind`: 생활간증·독서·기타 | ⚠️ **설교간증 없음** |
| 제출일 | 반별 마감일 `classes/<id>/due/<taskId>` | ✅ |
| 제목 (생활간증·독서) | `task.title` | ✅ |
| 제목 (설교간증) | 매칭된 글 제목 `post.title` | ✅ 분류 태그만 제거 |
| 제출자 13명, 고정 순서 | `QT_TARGET_NAMES` 와 **완전 일치** | ✅ |
| 본문 (문단 단위) | `post.content` | ✅ 문단 분할 필요 |
| 미제출 = 빈 셀 | `autoAssign` 미매칭 | ✅ |

### 3.1 메워야 할 구멍 두 가지

**(A) 설교간증 과제 종류가 커리큘럼에 없다.**
`js/assignments.js` 의 `kind` 는 `생활간증`/`독서`/`기타` 뿐입니다. 하지만 실제로는 매주
**설교간증**이 따로 제출되고 별도 취합문서로 묶입니다. 커리큘럼에 주차별 설교간증 과제를
추가해야 이 문서를 만들 수 있습니다. (`config.js` 의 `categorize()` 는 이미 `예배은혜나눔`
카테고리를 알고 있으므로, 게시글 쪽 분류는 이미 되어 있을 가능성이 높습니다 — P0에서 실데이터 확인.)

**(B) 출석·금요기도회·주일예배 데이터가 아예 없다.** → §6

## 4. 자동화 방식 — 양식에 표식을 심지 않는다

양식을 수정할 수 없으므로 `{{이름}}` 같은 토큰을 넣을 수 없습니다.
대신 **양식의 구조가 이미 충분히 규칙적이라는 점을 이용**합니다.

### 4.1 앵커 기반 셀 찾기

좌표를 하드코딩하지 않고, **라벨 셀을 앵커로 삼아 값 셀을 찾습니다.**

| 앵커(고정 라벨) | 값을 넣을 곳 |
| --- | --- |
| `제출일` | 같은 표에서 그 다음에 오는 셀 |
| `제목` | 그 다음 셀 |
| `제출자` | 그 다음 셀 |
| (앵커 없음) | 표의 **마지막 행 + `colSpan=4`** 셀 = 본문 |

라벨 문구가 바뀌거나 못 찾으면 **에러로 처리**합니다. 조용히 엉뚱한 칸에 쓰는 것보다 낫습니다.

### 4.2 표 복제로 인원 맞추기

양식의 **첫 번째 표를 프로토타입**으로 삼아 멤버 수만큼 복제합니다.
복제본은 셀 구조·`borderFillIDRef`·`paraPrIDRef`·`charPrIDRef` 를 그대로 물려받으므로
**서식이 원본과 100% 같습니다.** 표가 13개든 5개든 인원에 맞춰 나옵니다.

### 4.3 본문 문단 채우기

본문 셀 안의 **첫 `hp:p` 를 문단 틀로 복제**해 줄 수만큼 만들고, 각 복제본의 `hp:t` 에 한 줄씩
넣습니다. 원본 문단의 `paraPrIDRef`/`charPrIDRef` 가 복사되므로 본문 서식이 유지됩니다.
빈 줄은 텍스트 없는 문단 하나로 둡니다(문단 간격 유지).

### 4.4 손대는 것 / 안 건드리는 것

| 다루는 방식 | 대상 |
| --- | --- |
| **절대 안 건드림** | `Contents/header.xml` (글꼴·문단모양·표 스타일 전부), `settings.xml`, `version.xml`, `META-INF/*`, `Scripts/*` |
| 값만 교체 | `Contents/section0.xml` 의 `hp:t` 텍스트 |
| 구조 복제 | `hp:tbl`(표), 본문 `hp:p`(문단) |
| 재생성 | `hp:tbl/@id`, `hp:p/@id` (문서 내 고유값이라 복제 시 충돌 방지) |
| 삭제 | 수정한 문단의 `hp:linesegarray` (줄배치 캐시 — 한글이 열 때 다시 계산) |
| 갱신/삭제 | `Preview/PrvText.txt` 는 생성 평문으로 덮어쓰고 `Preview/PrvImage.png` 는 제거 |

> `linesegarray` 제거와 `cellSz/@height` 자동 재계산은 **한글에서 실제로 열어봐야 확정**됩니다.
> P1의 첫 검증 항목입니다(§8 리스크 1).

### 4.5 ZIP 재작성 규칙

`mimetype` 을 **첫 엔트리·무압축(STORED)** 으로 두고 나머지는 그대로 다시 담습니다.
양식에 있던 엔트리는 하나도 빠뜨리지 않습니다(`Scripts/`, `container.rdf` 포함).

## 5. 모듈 구성

```
assets/templates/11기-설교간증.hwpx     ← 내용을 비운 양식 껍데기 (§9 결정 2)
assets/templates/11기-생활간증.hwpx
assets/templates/11기-독서과제.hwpx
js/hwpx/zip.js        ZIP 읽기/쓰기 (JSZip 지연 로드, 구현 주입 가능)
js/hwpx/owpml.js      표/문단 복제, 앵커로 셀 찾기, 텍스트 치환   ← 순수 함수
js/hwpx/compile.js    취합 데이터 → 채워진 section0.xml           ← 순수 함수
js/hwpx/build.js      양식 fetch → 해제 → compile → 재압축 → Blob
js/hwpx/export-ui.js  내보내기 모달 (반·과제 선택, 사전 점검, 다운로드)
index.html            모달 마크업 + 대시보드 버튼
```

**설계 제약 두 가지** (계획 수립 시 정한 것, 그대로 유지)

1. `owpml.js` / `compile.js` 는 DOM·Firebase·JSZip 을 import 하지 않는 **순수 모듈**.
   → Node 테스트가 브라우저 파일을 **그대로 import** 하므로 CLAUDE.md 불변식 1의
   웹/Node 수동 동기화 문제를 새로 만들지 않습니다. (코드 복제 금지)
2. **ZIP 구현은 주입.** 브라우저는 CDN 의 `window.JSZip`(첫 클릭 시 지연 로드),
   Node 테스트는 devDependency `jszip`. 순수 모듈은 ZIP 을 모릅니다.

XML 은 `DOMParser` 없이 **문자열 기반 노드 슬라이싱**으로 다룹니다(Node 에서도 돌아야 하므로).
대상이 `hp:tbl` / `hp:tc` / `hp:p` 경계 찾기와 복제뿐이라 파서가 필요 없습니다.

### 5.1 데이터는 추가 조회 없이 재사용

대시보드가 이미 메모리에 갖고 있습니다.

| 필요한 것 | 이미 있는 것 |
| --- | --- |
| 반 → 멤버 | `memberList` + `memberClassId(m)` (`active` 필터) |
| 반 → 과제 | `effectiveTasks(courseId, classId)` (반별 마감일 반영) |
| 과제 → 매칭된 글 | `autoAssign[name][taskId]` → `{title, postDate, link, content}` |
| 수동 체크 내용 | `assignStatus[name][taskId]` → `assignContent(v)` |
| 반/과정 이름 | `classesById`, `coursesById` |

빌더는 순수 함수 한 개입니다.

```
buildTaskDoc({ task, classId, members, autoAssign, assignStatus })
  → { 제출일, rows: [{ 이름, 제목, 본문[] }] }        // 미제출은 제목·본문 빈 배열
```

정렬은 이름 `localeCompare(ko)` — 과제 완주 현황 표와 동일하고, 양식의 13명 순서와도 일치합니다.

## 6. 출석·과제현황표(4번 문서)는 별개 과제

표 2개에 38행×55열. **멤버 1명 = 4열**, 주차 1개 = 2행입니다.

```
과제행:  [생]  [큐티 일수 (colSpan=2)]  [독]
출석행:  [출]  [금 (colSpan=2)]         [주]
```

완료 여부는 글자를 지우는 게 아니라 **글자 스타일(`charPrIDRef`)로 표시**합니다.

| charPr | 스타일 | 의미 |
| --- | --- | --- |
| `13` | 검정 굵게 `#000000` | **완료** |
| `20` | 연회색 `#BFBFBF` | **미완료** |
| `32` | 연회색 + 취소선 | **해당 없음/제외** |

즉 자동화는 **셀의 `charPrIDRef` 를 13 ↔ 20 으로 바꾸고 큐티 칸에 일수를 쓰는 것**이 전부입니다.
양식을 전혀 건드리지 않는 아주 깔끔한 형태입니다.

**다만 지금은 만들 수 없습니다.** 6개 칸 중 3개의 데이터가 없습니다.

| 칸 | 데이터 | 상태 |
| --- | --- | --- |
| 생 (생활간증) | `autoAssign` | ✅ |
| 큐티 일수 | `memberQtData` 주차별 집계 | ✅ |
| 독 (독서) | `autoAssign` | ✅ |
| **출** (출석) | — | ❌ 수집·입력 수단 없음 |
| **금** (금요기도회) | — | ❌ |
| **주** (주일예배) | — | ❌ |

출석 3종은 게시판에서 긁을 수 있는 값이 아니라 **사람이 입력해야 하는 값**입니다.
→ 이 문서 자동화는 **출석 입력 기능이 먼저** 있어야 합니다. 이번 범위에서 제외하고
후속 과제로 둡니다(§9 결정 5).

참고로 이 표의 이름 행에는 `임채환 (반장)`, `한수종(총무)` 처럼 **직책이 붙어** 있습니다.
`/members` 에는 직책 필드가 없으므로, 자동화할 때는 **양식의 이름 행을 그대로 두고**
괄호를 떼서 우리 명단과 대조해 열 번호를 찾는 방식이 맞습니다.

## 7. 단계별 진행

| 단계 | 내용 | 완료 조건 |
| --- | --- | --- |
| **P0** 준비 | 양식 껍데기 3종 생성(§9 결정 2), 설교간증 과제를 커리큘럼에 추가, 실데이터로 `categorize()` 확인 | 껍데기 파일이 한글에서 정상으로 열림 |
| **P1** 코어 | `owpml.js` — 앵커 셀 찾기·표 복제·문단 복제·id 재생성·linesegarray 처리 | 픽스처로 Node 테스트 통과 + **한글에서 열어 서식 확인** |
| **P2** 패키징 | `zip.js` + `build.js` (해제→치환→재압축, mimetype 규칙, Preview 처리) | 생성 파일 엔트리 구성이 양식과 일치 |
| **P3** 데이터 | `buildTaskDoc()` — 과제별 취합 데이터, 설교간증 제목 분기 | 제출/미제출/본문없음이 테스트로 고정 |
| **P4** UI | 내보내기 모달(반·과제 선택, 멤버별 매칭 글 미리보기, 사전 점검 경고, 다운로드) | 로그인 상태에서 클릭 → 파일 저장 |
| **P5** 검증·문서 | 실데이터로 생성 → 기존 수작업 문서와 대조, README 작성 | **사용자가 한글에서 열어 기존 문서와 같은 모양 확인** |

P1 이 이 작업의 심장입니다. 여기서 한글 호환성이 판가름 나므로, **P1 이 끝나면 곧바로
사람이 한글로 열어보고** 다음 단계로 갑니다.

## 8. 리스크

| 리스크 | 영향 | 대응 |
| --- | --- | --- |
| `linesegarray` 를 지웠을 때 한글이 줄배치를 다시 계산하지 않음 | 문서가 깨져 보임 | **P1 최우선 검증.** 실패하면 문단 수를 바꾸지 않는 대안(본문 전체를 한 문단에 `lineBreak` 로) 검토 |
| 본문 길이가 바뀌어도 `cellSz/@height` 가 그대로라 표가 잘림 | 내용 잘림 | P1에서 긴 본문·짧은 본문 양쪽으로 확인. 필요하면 height 를 비우거나 추정치로 갱신 |
| `hp:tbl/@id`·`hp:p/@id` 중복 | 한글이 파일을 거부 | 복제 시 새 id 발급 + 중복 검사 테스트 |
| 문자열 기반 XML 조작의 엣지 케이스 | 문서 손상 | 자기닫힘 태그·중첩 표를 픽스처로 고정. 처리 후 XML well-formed 검사 |
| `mimetype` 순서/압축 위반 | 파일 열림 실패 | `zip.js` 에서 강제 + 재압축 결과를 테스트로 검증 |
| 과거 글의 `content` 가 빔 | 취합 누락 | 내보내기 전 사전 점검에서 건수 경고 + `backfill-content` 안내 |
| 자동 매칭 오류로 엉뚱한 글이 들어감 | 내용 오류 | 모달에서 **멤버별 매칭된 글 제목을 먼저 보여주고** 확인 후 다운로드 |
| JSZip CDN 장애 | 기능 정지 | 버전 고정 + 실패 시 명확한 안내 |

## 9. 결정 대기 항목

| # | 질문 | 기본안 |
| --- | --- | --- |
| 1 | 이번 범위에 **설교간증** 문서까지 포함? | 포함. 단 커리큘럼에 설교간증 과제를 먼저 추가해야 함 |
| 2 | 저장소에 커밋할 양식 파일 | **실제 제출 내용을 지운 껍데기**로 만들어 커밋. 받은 4개 파일은 실명 간증문이라 git 이력에 남기지 않는 편이 낫습니다. 껍데기는 서식(`header.xml`·모든 ID 참조)을 그대로 두고 텍스트만 비우므로 **양식은 그대로**입니다 |
| 3 | 문서 제목의 **기수**(`11기 제자 훈련반`)가 양식에 박혀 있음 | 반마다 양식 파일을 따로 둠. 한 셀만 반 이름으로 바꾸는 예외를 허용할지 확인 필요 |
| 4 | 파일명 규칙 | `11기_생활간증_20260503.hwpx` (양식 문서 제목 + 제출일) |
| 5 | **출석·과제현황표**(4번) | 이번 범위 제외. 출석 입력 기능이 생긴 뒤 후속 과제 |
| 6 | 내보내기 권한 | 관리자 = 전 반, 반 계정 = 자기 반만 (기존 스코프 그대로, 읽기 전용이라 **RTDB 규칙 변경 없음**) |
| 7 | 백지연 | 양식 13명에 없음 = `QT_TARGET_NAMES` 기준. 취합 대상도 동일하게 갈지 확인 |

## 10. 테스트

- **Node 단위 테스트** (`scripts/test/hwpx-*.test.js`, 기존 `node --test` 에 자동 편입)
  - `owpml.js`: 앵커 셀 찾기(라벨 없을 때 throw), 표 복제, 문단 복제, id 재생성, 이스케이프, 제어문자 제거, 자기닫힘 태그
  - `compile.js`: 픽스처 → 기대 출력, 표 개수 = 멤버 수, 미제출 시 빈 run 유지
  - `buildTaskDoc()`: 제출/미제출/본문없음/설교간증 제목 분기/정렬
  - 패키징: 엔트리 목록·`mimetype` 첫 엔트리·STORED·XML well-formed
- **픽스처**: 껍데기 양식에서 **표 1개만 남긴 최소 `.hwpx`** 를 `scripts/test/fixtures/` 에 커밋
- **devDependency**: `scripts/package.json` 에 `jszip` (테스트 전용)
- **수동 검증**: 실데이터로 생성한 문서를 한글에서 열어 **기존 수작업 문서와 나란히 비교**

CI 는 기존 `ci.yml` 그대로 통과해야 합니다 (`node --check` 전체 + `npm test`).

## 11. 다음 액션

1. §9 의 결정 1·2·3·7 확인
2. P0 착수 — 양식 껍데기 3종 생성 + 설교간증 과제를 커리큘럼에 추가
